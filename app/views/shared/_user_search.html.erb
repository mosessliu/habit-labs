<%= form_with(url: users_search_path, remote: true) do |f| %>
  <div class="form-group">
    <div>
      <%= f.text_field :Search_friends, class:"form-control", placeholder: "Search for a friend"%>
    </div>
    <div>
      <%= f.submit "Search", class: "btn btn-md btn-success"%>
    </div>
  </div>
<% end %>